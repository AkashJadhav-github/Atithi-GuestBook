<h1>Atithi</h1>
<style>
  h1 {
    background-color: orange;
    padding: 5%;
    font-family: "Noto Sans CJK SC Bold";
  }
</style>

<% if flash[:error] %>
    <div class = "error"><%= flash[:error] %></div>
<% end %>

<h2>New Guest</h2>

<%= render 'form', guest: @guest %>

<button><%= link_to 'Back', guests_path(current_user.id, current_user.email_id, current_user.name) %></button>
